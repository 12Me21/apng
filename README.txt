~~~!!!!! MAGIC INSTANT APNG GENERATOR !!!!!~~

I hope you are using linux...

$ make
$ ./apng-hack

*** DISCLAIMER! ***
ok so like,
this completely violates the png spec, but it will probably work, on pngs generated by *normal programs*
make sure all the frames are the same size (of course) and use the same encoding etc. and whatever
if it works, it works (output should be totally valid), and it's ~100x faster than, for example, imagemagick
otherwise, use a real apng encoder w

